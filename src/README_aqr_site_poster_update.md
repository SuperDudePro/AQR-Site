# AQR Site Poster Catalog Update

Replace:

`src/posterData.ts`

with the included `posterData.ts`.

This update adds the uploaded poster pairs to the site catalog:

- Not Just Answers
- Serious Math, Different Path
- Is That Reasonable?
- Weighting Is a Value Statement
- Know the User
- Ask Earlier
- Camus: Invincible Summer

The file paths assume the PNG and PDF files are already uploaded in `public/posters/` with the exact filenames referenced in `posterData.ts`.
